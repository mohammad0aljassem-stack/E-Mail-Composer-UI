# Feature flag for the canonical composer v1 (editor, composer lab, render API).
# Set to "true" to enable /composer-lab and /api/composer/render.
# Any other value (or unset) disables the feature — this is the rollback switch.
NEXT_PUBLIC_COMPOSER_V1_ENABLED=true
